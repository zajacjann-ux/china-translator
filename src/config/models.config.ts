/** Default chat model for voice and text translation. */
export const DEFAULT_TRANSLATION_MODEL = 'gpt-5.5';

/** Vision model for camera OCR — kept separate so translation upgrades do not affect camera. */
export const DEFAULT_VISION_MODEL = 'gpt-4o';

/** OpenAI Whisper speech-to-text model. */
export const DEFAULT_STT_MODEL = 'whisper-1';
